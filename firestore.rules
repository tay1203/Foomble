rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // The browser does not access Firestore directly. Cloud Functions use
      // the Admin SDK and remain able to access the database.
      allow read, write: if false;
    }
  }
}
